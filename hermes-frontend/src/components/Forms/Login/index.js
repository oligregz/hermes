import React from 'react';
import Button from '../../Button';
import Input from '../../Input';

const FormLogin = () => {
  return (
    <div>
      <Input />
      <Button />
    </div>
  );
}

export default FormLogin;
